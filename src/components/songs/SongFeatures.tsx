import React from 'react';
import styled from 'styled-components';
import { AudioFeatures } from '../../constants/types';

type Props = {
  audioFeatures: AudioFeatures,
}

const SongFeatures = (props: Props) => {

  return(
    <div>Features</div>
  );

}

export default SongFeatures;